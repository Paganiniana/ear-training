import { ForwardButtonComponent } from './forward-button.component';

describe('ForwardButtonComponent', () => {
  it('should create', () => {
    expect(ForwardButtonComponent).toBeTruthy();
  });
});
